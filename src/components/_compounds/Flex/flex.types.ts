import { ReactNode } from "react";

export type FlexItemProps = {
  as?: string;
  s?: Record<string, any>;
  children: ReactNode;
};

export type FlexRootProps = {
  as?: string;
  s?: Record<string, any>;
  children: ReactNode;
};
